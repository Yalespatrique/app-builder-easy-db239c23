{
  "name": "asterplay-android",
  "version": "1.0.0",
  "private": true,
  "description": "Native Android TV project — see README.md. Web preview is a static placeholder.",
  "scripts": {
    "build:dev": "mkdir -p dist && cp -r public/* dist/",
    "build": "mkdir -p dist && cp -r public/* dist/",
    "dev": "node preview-server.cjs"
  },
  "devDependencies": {
    "serve": "^14.2.6"
  }
}
